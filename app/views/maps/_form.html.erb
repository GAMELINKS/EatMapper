<%= form_with(model: map, local: true) do |form| %>
  <% if map.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(map.errors.count, "error") %> prohibited this map from being saved:</h2>

      <ul>
      <% map.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <p><%= form.label :title %></p>
    <p><%= form.text_field :title %></p>
  </div>

  <div class="field">
    <p><%= form.label :about %></p>
    <p><%= form.text_area :about %></p>
  </div>

  <div class="field">
    <p><%= form.label :image %></p>
    <p><%= form.file_field :image %></p>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
